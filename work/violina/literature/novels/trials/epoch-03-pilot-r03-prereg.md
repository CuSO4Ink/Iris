# Epoch 03 Pilot · Round 03 · Preregistration

- 状态：`COMPLETED — ONE ROUND STOPPED`
- 日期：2026-07-19
- 用户反馈：既有候选人物普遍显得闷、理性；要求更换人设，测试“超级热情黏人的攻”；标点不应长期只剩问号、句号和逗号；动态神态应更多穿插在对话实际发生的过程中。
- 写手、审校员、来源读者：统一冻结 `gpt-5.6-sol / high`。
- 生成：H/L 各一个全新零继承写手，各一次 3000—5000 汉字直写；不换种子、不补写、不建候选池。
- Canon：两个截面均为试验性新作，不创建 works 目录，不提交 StoryAuthority。

## 诊断基线

- Round 02 H 标点：`。244`、`，248`、`？34`、`！0`、`……0`、`——2`。
- Round 02 L 标点：`。271`、`，252`、`？22`、`！0`、`……0`、`——0`。
- 冻结 WriterSystemBaseline 已明确允许情绪改变话量、动作、攻击性、依赖与求助，也明确“情绪高点的话量须落成直接台词”；因此问题不是规范禁止强情绪。
- 实际收缩来自多层联合作用：成熟文学的默认标点归一化、`克制` 被模型泛化为低外显、两名限知主受的清冷／职业稳妥人设，以及 Round 02 对语气词“不按配额”的说明。结果是另一女性即使被写作活泼，声音仍被限知叙述的冷静节奏吸收。
- 神态不足并非硬闸门数量问题，而是对话常以连续引号完成一轮意义交换，再由一个动作总结情绪；脸、视线、呼吸、姿态和距离没有进入发话、改口、抢话与误听的实时因果。

## 冻结生成栈

1. `writer-system-baseline.md` 标记范围，逐字不改。
2. `writer-system-calibration-epoch-02.md` 标记范围，逐字不改。
3. 本轮对应 H/L GenerationFrame。
4. `epoch-02-r03-overlay.md` 的 `DOMAIN-SEPARATED EPISTEMIC CHANNELS` 标记范围，逐字不改。
5. `epoch-03-pilot-r02-overlay.md` 的 `ATTENTION-LED PROSE MODE` 标记范围，逐字不改。
6. 本轮 `EXPRESSIVE ATTACHMENT CHANNEL` overlay，逐字不改。
7. 输出边界。

## 唯一联合变量

`EXPRESSIVE ATTACHMENT CHANNEL — 高振幅黏人攻的具身发声通道`

它不是新增另一套总架构，而是对既有 `ATTENTION-LED PROSE MODE` 的一轮可撤销扩展，同时处理同一根因的三个表面结果：人物情绪被成熟化、标点被归一化、神态与对白分离。具体文本见 `epoch-03-pilot-r03-overlay.md`。

## 新 H/L 截面

- H：架空志怪；年上脾气直、怕热、并不擅长维持体面的女游医陆停云（38）为主受限知；年下成年狼妖护卫祝满（25）是超级热情、主动贴近、会撒娇抢话的攻。
- L：现代城市；年上情绪外露、生活凌乱的独立摄影师许雁回（40）为主受限知；年下成年花店老板孟小满（27）是超级热情、黏人、爱笑也会直接表达占有欲和不高兴的攻。
- 两篇均保持封闭女女关系、成年同意边界和他者性；黏人不等于无边界、无事业或永远愉快。

## 最相近实验、反证与指标

- 最相近的是 Round 02 对话低效、直接心理与动态观看的联合架构。新增量不是“多加几个感叹号”，而是让另一女性的高情绪振幅拥有不经主受冷静语体翻译的直接发声通道，并让标点、神态、距离和触碰共同承担话轮状态。
- 最强反证：强人设可能退化成吵闹、卖萌或表演式小狗；感叹号与省略号可能成为新配额；神态穿插可能变成每句对白后的舞台说明，反而提高等密度与 AI 感。
- 执行观察：记录 `！`、`……`、`——` 等是否在真实声音或中断中自然出现，但不设最低数量，不以符号多少判断成功。
- 人物观察：黏人攻须在全文因果中真实改变话量、距离、动作、判断和恢复速度；同时至少保有一次拒绝、转向自身事务或不配合主受的选择，不以“热情”抹掉主体性。
- 神态观察：检查动态表情与身体是否实际改变发话、停顿、抢话、误读或双方距离；不数描写次数。
- 硬闸门仍守事实、视角、人物、关系、时空和完整性。来源测试仍以每篇五名零上下文读者、中位数 `<50%` 且多数判为人写为基础线。
- 完成两篇硬闸门与来源测试后停止；不自动确认，不修改正式 Skill，不提交 Canon。

## 冻结结果

- H Candidate SHA-256：`3C23E29AD542C80EF70DE6BEC9DD6E24E2CAF9DE69105784F40FD942FC392DC2`；3604 汉字；标点 `！9, ？40, ——5, ……0`；硬闸门通过；来源 `42, 34, 58, 42, 43`，均值 `43.80%`，中位数 `42%`，Human `4/5`，`PASS`。
- L Candidate SHA-256：`8B05D9768D5AAFA417B2288D99ABCD146646A01A51EBD12190B5ED729B5672AB`；4756 汉字；标点 `！21, ？60, ——6, ……0`；硬闸门通过；来源 `42, 36, 38, 43, 38`，均值 `39.40%`，中位数 `38%`，Human `5/5`，`PASS`。
- 未修订、未补测、未运行确认、未提交 StoryAuthority。完整结论见 `epoch-03-pilot-r03-summary.md`。
