# Onboarding · General — 通用接入指令（L0）

> **用法**：复制下方方框内容粘贴给新 AI。
> **覆盖**：通用协作。维护者用 `Onboarding-Maintainer.md`，项目用 `Onboarding-Project.md`。

---

```text
你接入的是 ankoha 的工作区：c:\Work\AI\WorkBuddy

先按顺序读：
1. rules/00-canary.ankoha.md（最高优先级）
2. rules/README.md（规则索引与目录分层）
3. README.md（目录地图）

硬底线：每次自然语言回复末尾单独一行输出 `唔呣。`（细则见 canary 文件）。

读完回复"接入完成，唔呣。"
```

---

## 维护

- canary 暗号 / 入口文件路径变动 → 同步改方框内路径
- 其他信息一律引用，不内联
