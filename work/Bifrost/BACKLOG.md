# Bifrost · BACKLOG

> 待办清单 + 决策框架。顺手加，做完打勾。复杂任务可转成 `tasks/T-xxx.md`。

## 决策框架（阻塞后续，由人拍板 — AI 不替选）

> 以下三项是 L2/L3 的 Art Direction 核心决策，**AI 不预设候选、不替你选**。灵感靠 moodboard 沉淀。这里只列"拍板时该权衡哪些维度"，防止跑偏。三者互相牵制，宜一次性定调。

### D1 · 奇观焦点 —— ✅ 已决策（2026-07-02）：合成态「等离子恒星驱动天气」
定为**等离子恒星（复用 NS_Slime marching cubes）驱动天气/风暴/god-rays 的合成态**（见 LOG 19:45，提示词见 `CONCEPT.md` §3）。内核=等离子恒星 isosurface，满足"只做1个"硬约束；breakdown 价值=marching cubes 融合表面拆解（D3 展开）。

### D2 · 色彩基调 —— ✅ 已决策（2026-07-02）：冷神性
定为**冷神性**（冷白蓝巨星、肃穆神性光，pale azure/icy cyan），见 LOG 17:10。落地守则：瑰丽≠俗艳、冷调下控高光曝光避免生硬；LookDev/后期调色阶段稳定复现该基调。

### D3 · 前沿内核技术选型（L3 实现路径） —— 🚧 展开中，全过程见 `D3-KERNEL.md`
D1 已定等离子恒星 isosurface，D3 据此选实现路径。**权衡维度 / A·B·C 三方案 / UE5.8 落地 / 成本 / breakdown 全部迁入 `D3-KERNEL.md`**，本处只留指针。待选：A 轮廓局部化 / B 纯 shader 假体积 / C 转真体积（GaussianVolume）；~~Bifröst 能量桥降本提议~~已否决（LOG 21:20）。
> [!TODO] D3 拍板后：结论回填 `D3-KERNEL.md` §6 + 本条 + LOG + CONCEPT，并解锁 T-L3.1。

## 进行中

- [ ] 灵感 / moodboard 沉淀 —— 支撑 D1/D2/D3 拍板（人主导，AI 可按需协助搜集参考）

## 待办（执行路径 · 按 L1/L2/L3 三层组织）

> 依赖标记：`⟸ 依赖X` 表示需先完成 X。验收标准 = 「done 的判定条件」。

### L1 量产层（AI/MCP）
- [ ] **T-L1.1 岩石母材质规格 + 生成**
  - 依赖：D2 已定（冷神性），可开工
  - 做法：定义参数面板规格（目标色域=冷神性）→ 交 MCP 生成母材质 + MI → 人验收
  - 验收：母材质含可调参数面板（粗糙度/苔藓混合/风化等）；≥3 个 MI 变体；人工 LookDev 通过
- [ ] **T-L1.2 植被 / 天空球 / 远景 mesh 摆放流程验证**
  - 依赖：无（可先跑小样本）
  - 做法：小样本先跑通 AI 生成 → 摆放 → 验收的完整链路
  - 验收：跑通一个最小闭环（1 类植被 + 天空球 + 若干远景 mesh），流程可复现
- [ ] **T-L1.3 海面基础着色器脚手架**
  - 依赖：D1 已定（等离子恒星驱动天气），可开工；若海面需承接恒星光学效果，脚手架预留接口
  - 做法：搭基础海面着色 shader 脚手架，参数化留扩展位
  - 验收：基础波形 + 反射/折射可跑；参数面板预留 L3 内核接入点

### L2 审美把控层（人）
- [ ] **T-L2.1 moodboard**：收集北欧奇幻巨构参考，支撑 D1/D2 拍板 ⟸（无，最先做）
  - 验收：形成可对比的参考集，D1/D2/D3 得以定调
- [x] **T-L2.2 镜头 / 构图设计**：主机位定为 **⑤前景剪影环境镜**（人物剪影近景 + 恒星在背景发光），2026-07-06
- [ ] **T-L2.3 LookDev 与后期调色** ⟸ T-L1.*（有资产可看）+ D2
  - 验收：整体画面达成 D2 基调；调色链路可复现

### L3 前沿内核层（人）
- [ ] **T-L3.1 奇观焦点内核实现** ⟸ D3（D1 已定，D3 技术选型待展开）
  - 验收：内核在场景中稳定运行；具备可迁移性；性能可接受
  - 备注：硬约束 —— 只做 1 个，做深不摊大

### 收录准备（对齐 Portfolio）
- [ ] **T-P.1 全程留 breakdown 素材**（贯穿所有阶段，非末尾补）
  - 内容：节点图 / 分层 / 参数演示 / 性能数据
  - 验收：每个关键产出同步留拆解素材，满足 Portfolio 收录硬门槛

## 依赖顺序速览

```
D1 已定（等离子恒星驱动天气） ──┬─► T-L1.3 海面脚手架（可开工）
D2 已定（冷神性）              ├─► T-L1.1 岩石母材质（可开工）
                                ├─► T-L2.2 镜头构图（可开工）
                                └─► D3 待展开 ──► T-L3.1 内核实现
T-L1.2 植被/天空球（可并行，无阻塞）
T-L1.* 产出 ──► T-L2.3 LookDev/调色（已知目标基调=冷神性）
T-P.1 breakdown（全程贯穿）
```

## 已完成（近期，便于回忆）

- [x] 立项：作为 AIEffectFoundry 方向3 首个 diorama 实例（2026-07-02）
- [x] 定位：风格化巨构 > 写实，北欧神话 + 宏大奇幻基调（2026-07-02）
- [x] 三层职责分工骨架 L1/L2/L3 定义（2026-07-02）
- [x] 抽象重构：剥离 AI 擅自具象的效果/色彩候选，收敛为决策框架 + 执行路径任务清单（2026-07-02）
- [x] T-L2.2 镜头/构图：主机位定为⑤前景剪影环境镜（2026-07-06）

---

完成超过 2 周的项移除；有保留价值的写进 LOG.md。
