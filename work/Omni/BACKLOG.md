# Omni · BACKLOG

## 进行中

- [ ] 审阅 `SYSTEM-MAP.md`，批准第一版模块的拆分、合并和依赖方向。

## 下一步

- [ ] 确定项目级目标平台、帧预算、画质档位和视觉基线。
- [ ] 选择一个真实模块 Gate，只初始化其对应的具体 UE 项目，并添加 `--ue`。

只保留尚未解决且可以执行的工作。`/checkpoint` 会在持久事实写入 `AI-BRIEF.md` 或
`LOG.md` 后移除已完成事项。
