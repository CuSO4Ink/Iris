{
    "endpoint":  "http://127.0.0.1:8000/mcp",
    "ok":  true,
    "data":  {
                 "success":  true,
                 "output":  "x.set_asset(asset, reset_existing_override_parameters=True) -\u003e None\r\r\nSwitch which asset the component is using.\r\nThis requires Niagara to wait for concurrent execution and the override parameter store to be synchronized with the new asset.\r\nBy default existing parameters are reset when we call SetAsset, modify bResetExistingOverrideParameters to leave existing parameter data as is.\r\r\n\r\r\nArgs:\r\r\n    asset (NiagaraSystem): \r\r\n    reset_existing_override_parameters (bool):",
                 "result":  "None",
                 "execution_time_ms":  19.106599807739258
             },
    "action":  "tool.call"
}