# slime · BACKLOG

## Doing

- [ ] 固定相机复查中央差分法线与新材质参数；若轮廓仍明显分层，再实现真正的 Grid3D 后处理，
  不再继续放大 SDF 支持半径。

## Next

- [ ] 用已记录的 120 帧基线复测最终版本；优先检查 Niagara Compute 1.28 ms 与 Marching Cube 262144-cell 路径。
- [ ] 完成引用检查后，决定是否保留 `NS_SlimeLocal`、`NS_Boid` 和未接入的材质/调试资源。

Keep only unresolved, executable work. `/checkpoint` removes completed operations after durable
facts are reflected in `AI-BRIEF.md` or `LOG.md`.
