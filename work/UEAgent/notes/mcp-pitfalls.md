# MCP evidence ledger

This is an evidence index, not mandatory operating context. Stable rules live in the relevant
`skills/ue-mcp-workflows/references/` document.

## Record policy

- Use `SOURCE-ID` for imported records and `DOMAIN-YYYYMMDD-SLUG` for new UEAgent records.
  Never allocate another global K-number; parallel projects already collided on K11/K17/K18.
- **Observed** means a real incident without an isolated reproduction.
- **Verified** means a Probe or controlled rerun established the behavior and postconditions.
- **Conditional** means verified only for the named UE/plugin build.
- Promote behavior, not anecdotes, into an SOP. Keep source/version provenance here.
- Evidence status describes the recorded observation, not current-target availability. A source
  fix and a deployed/rebuilt/live-verified fix are different facts. Unless stated otherwise,
  imported UE 5.8.1 records retain their original machine/build scope and need target revalidation.
- Lead changed entries with their current disposition; keep obsolete routes and measurements as
  historical evidence. Save-boundary failures are defects to diagnose, not permission to create
  unrelated changes for a token. Stable handling belongs in the domain SOP.

## Evidence index

The 2026-09-06 runtime verification resolved the exact-object fallback, shared-scratch healing,
data-view compile side effect, input/save race, and scratch invalidation cases below. Complex
production-Blueprint incidents remain scoped to their original assets. Current target work is in
`BACKLOG.md`; full evidence is in `runtime-verification-20260906.md`.

| ID | State | Evidence and current route |
|---|---|---|
| `CORE-20260709-PAYLOAD` | Verified | Large base64/image results were truncated above the gateway despite larger output limits. Prefer user viewport or a tool-owned file path. Promoted to Core/Scene. |
| `UEA-K7` | Verified | Changing existing UObject array elements while resizing caused ambiguous structural diffs. Full staged read-modify-write passed. Promoted to Core. |
| `UEA-K8` | Observed ×2 | `ProgrammaticToolset` could not pythonize material-property enums such as `MP_BaseColor` and `MP_PixelDepthOffset`; direct typed calls worked. |
| `UEA-K9` | Observed | `list_properties` exposed `MaterialExpressionCustom.showCode`, but `set_properties` rejected it as read-only. |
| `UEA-K10` | Observed ×2 | `AssetTools.get_referencers` raised on an empty result instead of returning an empty list. Exact-path cleanup plus `exists=false` was used. |
| `OCEAN-K11` | Observed | `MaterialTools.get_property_input` returned `expression` as a ref-path string although the schema described an object reference. Consumers must accept either shape. |
| `OCEAN-K12` | Observed | `TextureTools.get_size` rejected render targets and volume textures; `ObjectTools.get_properties` supplied dimensions/format. |
| `OCEAN-K13` | Observed | `find_assets.asset_type` rejected bare `NiagaraSystem`; omit an unverified class filter and verify returned classes. |
| `OCEAN-K14` | Observed | Niagara compile-state query timed out on `NS_InfiniteMesh` while summary/topology/input/renderer reads worked. Treat timeout as unknown and use narrower diagnostics. |
| `OCEAN-K15` | Observed | Large Niagara graph text containing escaped HLSL comments produced invalid gateway JSON. Return a smaller payload or extract raw text without JSON parsing. |
| `OCEAN-20260803-FIND-ACTORS-REQUIRED-EMPTY` | Observed | Live `SceneTools.find_actors` required `tag` and `collision_channels` even when both filters were unused; discover the call schema and pass `tag=""`, `collision_channels=[]`. |
| `BIFROST-20260805-LANDSCAPE-PROPERTY-ENUM-TIMEOUT` | Observed | `ObjectTools.list_properties` on a loaded 505x505 Landscape actor exceeded the gateway timeout, while `LandscapeService.ListLandscapes` returned its resolution, transform, material, and layers in a bounded response. Use the typed Landscape service and targeted property calls instead of enumerating the full actor. |
| `OCEAN-20260803-FUNCTION-EXPORT-TIMEOUT` | Observed | `MaterialNodeService.ExportFunctionGraph` on the 101-node coastline function consumed a core until the 120 s gateway timeout. Use the saved sidecar plus targeted typed node reads for small edits instead of exporting the full graph live. |
| `MATERIAL-20260803-VIBEUE-LIST-EXPRESSIONS-TIMEOUT` | Observed ×4 | `MaterialNodeService.ListExpressions` recursively expanded native function internals and timed out; `ListConnections` also exceeded 60 s on a 177-node SingleLayerWater material. Official `MaterialTools.get_expressions`/`get_expression_inputs` returned top-level refs and targeted wiring in seconds. Prefer those official bounded reads. |
| `MATERIAL-20260805-VIBEUE-EXPRESSION-ID-FALLBACK` | Observed | `MaterialNodeService.GetExpressionPins` and `ListExpressionProperties` accepted a short UObject name or full `refPath` but returned an unrelated first expression (`SmoothStep`/`CoastMask`) with success. Use VibeUE node IDs only when returned by the same service call; for persistent UObject refs, use official `MaterialTools.get_expression_inputs` and `ObjectTools` properties. |
| `MATERIAL-20260809-VIBEUE-BATCH-CONNECT-FIRST-ONLY` | Observed / recorded UE-plugin build | `MaterialNodeService.BatchConnectExpressions` received 11 valid heterogeneous links and returned `1`, but independent pin readback showed only the first link applied. Its displayed target names also differed from official callable names (`Clamp.Input` required the unnamed first input; TextureSample `MipLevel` was `Level`). Use official `MaterialTools.get_expression_input_names` plus targeted `connect_expressions` for these pins, then read back every link. |
| `MATERIAL-20260804-MI-UPDATE-IMPLICIT-SAVE` | Verified / recorded UE-plugin build | In controlled single-variable A/B runs, `MaterialEditingLibrary.set_material_instance_scalar_parameter_value` returned `false` but changed the scalar and advanced the `.uasset` without an explicit save. The first run also refreshed the ReflectCache sidecar; a later run left that sidecar stale. Treat the setter as a save boundary, independently live-read the value, never retry from its boolean alone, and do not assume the cache hook ran. |
| `MATERIAL-20260804-PACKAGE-MODIFY-IMPLICIT-SAVE` | Observed | After unsaved material graph edits, `Package.modify(true)` plus two material-instance scalar setters caused both the parent material and instance `.uasset` timestamps to advance and both packages to read back clean, without an explicit save/update/refresh call. Treat this combined path as a save boundary until isolated; do not use it to preserve a visual-approval checkpoint. |
| `MATERIAL-20260804-RECOMPILE-DEFERRED-SAVE` | Observed / recorded UE-plugin build | Setting one `MaterialExpressionVolumetricAdvancedMaterialOutput` property and calling `MaterialEditingLibrary.recompile_material` returned with the parent material Dirty=true; about two seconds later it was clean and both `.uasset` and ReflectCache sidecar timestamps had advanced, without an explicit save. `GetMaterialDiagnostics` is read-only by source contract. Treat this edit/recompile path as a possible deferred save boundary and keep an exact rollback value. |
| `MATERIAL-20260807-MI-STATIC-PARAM-REFRESH` | Observed / recorded UE-plugin build | After adding, compiling, and saving connected `StaticSwitchParameter` nodes to a parent material, `MaterialInstanceTools` still reported the new names absent. Reassigning the instance to the same parent refreshed its hierarchy; all four switches then read back `true`, while existing active scalar overrides retained their values. Treat a same-parent reassign as an instance mutation/save boundary and verify active overrides before saving. |
| `CORE-20260805-PROGRAMMATIC-RUN-LATE-CHECK` | Historical / execution route removed | `ProgrammaticToolset.execute_tool_script` executed top-level mutations before rejecting a missing `run()`, leaving changes despite an outer error. Current handling: this Gateway execution route is removed; use a typed queued operation. Preserve this incident as evidence that a late validation error can follow effects, not as instructions for authoring another Programmatic script. |
| `CORE-20260805-PYTHON-READ-AUTOSAVE` | Historical / execution route removed | Recorded source and live incidents showed `execute_python_code` saving all dirty content/world packages before even read-only Python. Expression-path lookups persisted the ocean material, instance, map and unrelated packages; a later run skipped saving because of a prior-crash guard. Current handling: Python execution is not exposed by the UEAgent Gateway and is not a recovery route. Use typed reads; account for exact externally saved packages and never automatically revert a mixed save containing user work. |
| `CORE-20260806-PROGRAMMATIC-DIRTY-SCAN-TIMEOUT` | Historical / execution route removed | A project-wide `/Game` enumeration with per-asset dirty queries exceeded 50 seconds through ProgrammaticToolset; exact-path typed queries stayed fast. The Programmatic execution route is now removed. Keep current checks bounded to the level and declared touched assets. |
| `SCENE-20260809-CONCURRENT-SAVE-CAPTURE` | Observed | A separate MCP `AssetTools.save_assets` call began about four seconds after a component property edit and saved both its target PCG asset and the already-dirty production map, thereby persisting the unrelated component edit too. Attribute a moved disk sentinel from exact logs, never auto-revert a mixed concurrent save, and restore only the exact owned property; enforce a one-writer boundary before future mutations. |
| `CORE-20260807-GATEWAY-ORPHAN-MEMORY-EXHAUSTION` | Observed incident / historical process guard | An orphan Windows PowerShell 5.1 `script.execute` process reached 100.624 GiB working set on a 127.92 GiB host; memory returned after exit. Event 2004 recorded other large PowerShell allocations, but the exact retained-allocation cause was not isolated. A historical 2 GiB private-memory ceiling and hard process deadline were tested, then retired with the old execution path; current Gateway does not expose those controls. It retains cancellation-aware bounded JSON/SSE reads, a 64 MiB response ceiling, and daemon request/response/idle bounds. Inspect the exact process and its ownership before attributing memory pressure to UE; do not broadly kill processes. Historical test measurements belong to their recorded revision. |
| `CORE-20260812-UE581-JSON-TOOLS-CALL` | Verified / recorded UE 5.8.1 transport; reader retained | Ordinary native MCP `tools/call` returned `application/json` while progress calls used SSE. The former 8 KiB PowerShell loop was replaced by cancellation-aware 80 KiB .NET reads with a 64 MiB response ceiling. Recorded runs handled 8 MiB JSON in about 0.93 s and SSE in about 1.0 s. Current regression covers both formats and session recovery; historical process-memory and hard-kill guards are no longer present. |
| `CORE-20260812-PROJECT-TARGET-LAUNCH` | Verified incident / historical Bootstrap launch behavior | After an engine patch upgrade, a stale generic editor loaded mismatched renderer binaries and failed on a missing shader include; a freshly built project target passed. Current handling: build the matching engine/project editor and restart it before runtime verification. `install_engine.ps1` builds the engine editor unless skipped; Bootstrap only writes/checks project binding and launches no process. |
| `CORE-20260808-SCRIPT-BACKEND-MISMATCH` | Historical / route removed | `ProgrammaticToolset.execute_tool_script` and Unreal Python are different backends; sending Python to the former timed out. UEAgent no longer exposes either Gateway action. Add a typed operation or stop. |
| `CORE-20260809-GATEWAY-FULL-TOOLSET-NAME` | Verified / recorded gateway + EditorToolset build | `tool.call -Toolset EditorToolset -Tool EditorAppToolset.SetCameraTransform` failed before dispatch with `Toolset 'EditorToolset' not found`. The registered pair `-Toolset EditorToolset.EditorAppToolset -Tool SetCameraTransform` succeeded, and a separate `GetCameraTransform` readback matched exactly. Gateway calls require the full registry toolset name plus the tool name relative to that toolset; a plugin/module short name is not an alias. A `Toolset not found` response is pre-operation failure, not `RESULT_UNKNOWN`. |
| `CORE-20260810-DOCTOR-MEMORY-GUARD-PASSTHROUGH` | Historical / parameter removed | The earlier Doctor failed to forward an SSPR-specific 1 GiB process ceiling and silently used the then-default 2 GiB limit; forwarding was verified on that revision. Current Doctor and Gateway expose no `ProcessGuardMaxPrivateMemoryMB` parameter. Do not copy the retired switch. Use bounded projections and current transport limits; the historical ceiling never made full RT/SimCache payloads appropriate. |
| `CORE-20260813-ASSET-FOLDER-READ-AUTHZ` | Observed / recorded UE-plugin build | `AssetTools.list_folders` was described with `effect=read`, but a direct Gateway call was rejected with `RELIABLE_COMMAND_REQUIRED`; discovery effect metadata is not authorization. For empty-folder creation, use reliable `create_folder` with the tool name relative to the full toolset, then verify the terminal receipt plus exact disk path; submit folder-list reads through the reliable queue until the direct read allow-list includes this tool. |
| `BLUEPRINT-20260814-CDO-PACKAGE-SNAPSHOT-GAP` | Verified / exact-object route verified in 2.0.1 | The original package-root snapshot missed changed CDO soft refs. Protocol 2.0.1 no longer substitutes a primary asset for a missing explicit object. The returned CDO scope plus package passed native identity tests and live InitialLifeSpan changes, controlled save and reload. Package-only coverage remains shallow; declare the actual affected object. Complex inherited-component behavior is a separate case. Never reparent just to obtain a save token. |
| `BLUEPRINT-20260814-NATIVE-COMPONENT-CDO-DIRTY-LOOP` | Verified / Abyss Kinesis | Direct writes to a Blueprint generated CDO's native inherited `SkeletalMeshComponent` persisted to disk but left generated/transient component state dirty, producing a compile/save loop. Restore the source component defaults, express the runtime override in an authoritative Blueprint graph such as `SetOverridePostProcessAnimBP`, save once, then fully reload the Editor/package; the post-restart state cache was clean. |
| `CORE-20260814-SNAPSHOT-PACKAGE-SCOPE-SYNTAX` | Verified / Abyss UEAgent | `ueagent_snapshot` rejected `package:/Game/...` as an invalid package/object scope. Package snapshots use the bare long package name `/Game/...`; only exact object scopes accept the `object:` prefix. |
| `ANIMATION-20260814-VIBEUE-ANIM-GC-SCOPE` | Verified / Abyss Kinesis | VibeUE `AnimSequenceService` calls that received only a package lease intermittently failed `UEditorAssetLibrary::LoadAsset` after a save/GC cycle; `PreviewPoseDelta` additionally returned `true` while its out-result reported load failure, so a later `BakePreviewToKeyframes` found no active preview. For reliable animation edits, pass the full `Asset.Asset` object path to VibeUE and declare both the bare package scope and exact `object:/Game/...Asset.Asset` scope so preflight keeps the UObject loaded. Prefer the stateless `ApplyBoneRotation` path over a preview state split across queued commands. |
| `CORE-20260808-PIE-PYTHON-GLOBAL-REFERENCE` | Historical / route removed | Persistent Unreal Python globals retained PIE UObjects and caused a teardown assert. UEAgent no longer exposes Python execution; use typed bounded reads instead. |
| `NIAGARA-20260808-SCREENSPACE-FIXED-CAMERA` | Verified / SSPR P0 Gate | A low-load Gather marker returned Aux.A=1 everywhere while NeighborQuery verbose logs showed valid PreStage/PostStage and sorting. The inherited viewport camera did not cover the component. At a fixed camera, SimCache showed 512/512 finite on-screen ScreenUV samples and positive depth, and the same chain produced positive counts and production moments. A screen-space runtime Gate must record/set a deterministic camera and prove projected samples are valid before classifying a zero RT. |
| `NIAGARA-20260808-RAW-READ-NORMALIZATION` | Verified / recorded UE 5.8 Python API; execution route removed | `RenderingLibrary.read_render_target_raw(world,target,True)` normalized an absolute Gather marker from its real `1..55` range to `0..1`, creating a false `CellCountZero` classification. Use `False` for markers, counts, density, depth, and numerical Gate statistics; report the normalization mode with the result. |
| `NIAGARA-20260808-RAW-AREA-WIDTH-HEIGHT` | Verified / recorded UE 5.8 Python API; execution route removed | `RenderingLibrary.read_render_target_raw_pixel_area(world,target,min_x,min_y,arg5,arg6,False)` interprets `arg5/arg6` as Width/Height despite MaxX/MaxY-style API naming. A 2048×256 strip at `y0=256` is `(0,256,2048,256)`, not `(0,256,2048,512)`. Use bounded strips and validate the returned element count exactly. |
| `NIAGARA-20260808-RT-READBACK-AGGREGATE-IN-UE` | Verified / SSPR 2048² Gate B | Returning two 2048² RGBA pixel arrays through Python→MCP→PowerShell would multiply compact GPU data into millions of wrappers and a huge JSON/object graph, recreating the PowerShell memory-exhaustion failure mode. Gate B succeeded by reading eight 2048×256 strips inside isolated UE Python, aggregating statistics there, deleting each strip, and returning only bounded summaries. Never JSON-serialize full RT pixels; for visual evidence let UE write a screenshot and return its path. |
| `NIAGARA-20260808-PIE-DI-CLONE-GENERATIONS` | Verified / recorded UE 5.8 World Partition SIE | Reinitializing the PIE Niagara component switched User DI bindings across clone generations (`_0/_1` to `_2/_3`, with later stale generations still visible). Configuring the old generation left the actual PIE World RTs at 2048². After every reinitialize, resolve the current User Variable DI refPaths again and verify the actual world-owned render-target dimensions; numeric UObject suffixes are not identities. |
| `SCENE-20260808-POST-PIE-PYTHON-PRESAVE` | Verified by editor log and binary recovery | A Python call restored a World Partition Niagara component but left its external Actor package dirty. The next `execute_python_code`, intended only to restore the viewport camera, logged `InternalPromptForCheckoutAndSave`, saved that external Actor to Content, and only then executed the script. This is the concrete post-PIE form of `CORE-20260805-PYTHON-READ-AUTOSAVE`: do not issue another Python read/cleanup while an external Actor may be dirty. Use a verified typed path or exit UE and restore the one exact package after checking no unrelated dirty state. The `.umap` can remain byte-identical while `Content/__ExternalActors__` changes. |
| `BIFROST-20260804-VOLUME-POST-EDIT-CHANGE` | Observed | A synchronous Interchange VolumeTexture reimport completed before Python raised because `VolumeTexture` exposes no `post_edit_change()` wrapper. Treat this as possible partial mutation: do not retry; independently read back import source, dimensions, settings, and dirty state. The explicit property writes already mark the asset dirty. |
| `OCEAN-20260803-OBJECT-PROPERTY-CALL-SHAPE` | Observed | `ObjectTools.get_properties` requires `instance={refPath}` rather than an `object` path, while `set_properties.values` is a serialized JSON string. Use the `detail=call` schema before property reads/writes. |
| `OCEAN-20260803-MATERIAL-DIAGNOSTICS-D3D12-CRASH` | Observed | A live `MaterialNodeService.GetMaterialDiagnostics` call was dispatched; 3.4 s later UE 5.8 crashed during BasePass in `FD3D12ResourceBinder::SetTexture` with `EXCEPTION_ACCESS_VIOLATION`. The concurrent client and causality are not isolated, so avoid this broad diagnostic on the live SingleLayerWater chain; use saved graph cache plus targeted function-node reads and ordinary dirty/compile readback. |
| `PERF-K16` | Rejected | `PerformanceService.frame_timing` reported multi-second game-thread values inconsistent with render/GPU timing. Do not use as performance evidence until an independent trace agrees. |
| `NNE-K17` | Observed | `AssetImportTask.save=false` did not keep a referenced NNE import transient; treat NNE import probes as potentially persistent and clean exact paths. |
| `NNE-K18` | Verified | Bulk deletion of a still-referenced Neural Profile chain crashed UE 5.8. Safe order: detach/read back → material → profile → model data → folder, verifying each deletion. |
| `SCENE-K19` | Observed | Nested `SceneTools.add_to_scene_from_class` stalled for 120 s with no Actor; the direct typed call succeeded in 17.5 s. Prefer direct creation until isolated otherwise. |
| `SSPR-K11` | Verified profile | Bare `CreatePin` plus manual signature rebuild produced Niagara pin/signature index mismatch and compile/UI Array asserts. The verified `niagara-authoring` profile exports the required UE 5.8 APIs and routes VibeUE through `RequestNewTypedPin`; use that profile for dynamic pin mutation. |
| `SSPR-K17` | Historical / route removed | Calling `NiagaraScratchPadService` inside `ProgrammaticToolset` timed out with no graph mutation. A Python route once completed, but it is no longer exposed; require the typed top-level service. |
| `SSPR-K18` | Observed | `ApplyChanges=false` plus empty compile messages hid the real rejection in `LogTemp`: stale scratch copies contained duplicate anonymous MapGet pins. Inspect `LogTemp: Error: ApplyChanges` before assuming asynchronous compile. |
| `SOURCE-UEAGENT-878558E-NIAGARA-AUTHORING` | Verified / UE 5.8.1 | Remote generic Niagara authoring overlapped local embedded-script/cache changes in `UNiagaraScratchPadService`. The resolved composite is under `patches/niagara-mcp-authoring/vibeue/vibeue-ueagent-authoring.patch`; it replaces, rather than layers on, the core VibeUE patch and requires the matching engine export patch. The rebased patch sequence, full `AbyssEditor` build, live doctor capability probe, and reliable no-write command flow passed on official UE 5.8.1. |
| `LIGHTNING-K01` | Verified / UE 5.8.1 | Niagara Custom HLSL in CPUSim goes through the HLSL→VectorVM translator (ES2 profile). `break` in a loop hangs the VVM compile job for minutes (asset editor blocks on "pending compile jobs"); integer `%` is rejected outright ("not an allowed operation on ES 2"); `active` is a reserved word. Write constant-bound loops with weight masks instead of `break`, and avoid integer division/modulo. |
| `LIGHTNING-K02` | Verified incident / fix packaged; target recheck required | The ApplyChanges validator rejected engine-generated hidden `Input + None + bNotConnectable` pins as duplicates when a MapGet had multiple typed reads. The skip is present in the current authoring composite. The earlier `E2AE5C63...` package identifier is historical. Adopt the current profile through `install_engine.ps1`, rebuild/restart, and validate the typed operation on the target; re-running project Bootstrap does not install the fix. |
| `LIGHTNING-K03` | Verified / recorded UE 5.8.1 save race | Token issuance required observed change, verified outcome and eligible dirty packages; pre-existing dirty state required `allow_preexisting_dirty_save=true`. Stack-input edits triggered asynchronous Niagara recompiles after the receipt and invalidated snapshots before saving. Current handling: inspect the settled compile/package generation and account for intervening changes. Do not bump a description or toggle a setting solely to obtain a token. If no task-required mutation remains, fix the typed save lifecycle or use an explicitly authorized exact-asset manual save. |
| `LIGHTNING-K04` | Verified / UE 5.8.1 source | Niagara dynamic-material-parameter slot 0 attribute is `Particles.DynamicMaterialParameter` (no `0` suffix; slots 1-3 have suffixes, see NiagaraStatelessCommon.cpp). Writing `...DynamicMaterialParameter0` silently creates a custom attribute nobody binds; the material's Dynamic Parameter node then reads the unbound default (1,1,1,1), which zeroed our reveal mask and made the ribbon fully transparent. Also: `AddNode` only accepts MapGet/MapSet/If/Input — use `AddCustomHlslNode`; and envelope `outcome=succeeded` does not imply tool-level `bSuccess` — check both in driver scripts. |
| `LIGHTNING-K05` | Verified / UE 5.8.1 | VibeUE scratch `ConnectPins`/`DisconnectPin` have NO `INPUT`/`OUTPUT` sentinel — node ids are GUIDs only (`FindNodeByGuid`); resolve the real Input/Output node ids via `ListNodes` (call with the module name) before wiring. All scratch modules created on one emitter share ONE scratch graph (Input/Output nodes are shared; ListNodes/ListConnections return the union). Driver scripts must check plain-boolean `result=false` as well as `result.bSuccess` — a missing sentinel wire fails silently otherwise. Symptom of a missing MapSet backbone wire: compile stays clean, but the written attribute never reaches the output map. |
| `LIGHTNING-K06` | Verified / UE 5.8.1 | Editor-side Niagara reads (GetEmitterTopology/GetScriptStackTopology/GetEmitterInputValues/compile state) can all look healthy while the runtime emitter instance spawns zero particles — a corrupted runtime binding after many same-session structural edits. Decisive probe: `NiagaraToolset_Component.GetRuntimeState` in PIE. Fix: RemoveEmitter + AddEmitter (Minimal template), then re-attach scratch modules via AddModule(moduleAsset=existing pad script) and re-add renderers; CreateScratchModule after removal orphans the old pad and suffixed `_1` copies mislead later pin ops. |
| `LIGHTNING-K07` | Verified / UE 5.8.1 | The editor world does not tick when the viewport is not realtime: system age stays 0, effects look dead, and viewport screenshots are invalid evidence. Run `EditorAppToolset.StartPIE` for any runtime truth (component path `/Game/<Map>/UEDPIE_0_<Map>.<Map>:PersistentLevel.<ActorObjectName>.<Component>`), and set PlayerStart `look_at` BEFORE StartPIE because the pawn camera pose is fixed at spawn. |
| `LIGHTNING-K08` | Verified / UE 5.8.1 | `AddUserVariables` DefaultValue must be one FNiagaraExt_InstancedValue: `{struct={refPath}, value={...}}` with the inner struct fields (FNiagaraPosition is FVector3f `{x,y,z}`); split `defaultValue`/`value` keys fail JSON conversion. Position defaults read back as `{}` regardless of content, so verify functionally via runtime bounds. An early malformed-payload SetVariable once stored garbage Position overrides (fixed by an explicit-zero rewrite). Position-typed override variants exist in TWO payload sizes: canonical 24-byte FVector (editor UI / serialization, see GetCurrentParameterValue) and 12-byte FVector3f (toolset writes); SetOverrideParameterStoreValue accepts both. The stock `EnsureOverrideParametersConsistent` ensure (NiagaraComponent.cpp) memcmp'd sizeof(FVector)=24B against the variant buffer, overrunning 12-byte payloads — fixed engine-side to a size-aware comparison (IRIS-FIX marker, uncommitted in engine working tree; flow into the patch pipeline when convenient). |
| `LIGHTNING-K09` | Verified / recorded UE 5.8.1 token package mismatch | `ueagent_save.packages` must equal the token's embedded package list; the job-level package list can be broader. Later mutations/compiles/saves can invalidate the token, and save receipts report outcome and persistence. A toggle off then on with net `no_change` issued no token. Current handling: preserve the exact token/package set and investigate invalidation; do not manufacture a mutation to force another token. |
| `LIGHTNING-K10` | Verified / UE 5.8.1 | Emitter `bLocalSpace=false` (default) plus a location-less spawn puts all particles at world origin — actor-placed systems render off-camera. Flip via `SetEmitterData` (param name is `emitter`, not `emitterRef`; round-trip the full GetEmitterData `propertyValues` blob with `"bLocalSpace": true`). With local space, empty anchor user-params anchor the bolt at the actor. |
| `LIGHTNING-K11` | Verified / UE 5.8.1 | Scratch-pad `MapGet` nodes have two roles under one nodeType: the module's input map (pins named Module.*/User.* read call-site inputs; adding `Particles.Age` there creates a zero-valued module input) vs the pad-level shared attribute MapGet (attribute reads work). Keep attribute pins on the pad-level MapGet and verify ListConnections `fromNodeId`. Separately: ObjectTools.set_properties on a material DynamicParameter DefaultValue requires R/G/B/A keys — X/Y/Z/W keys silently write garbage (NaN) into the FVector4. |
| `LIGHTNING-K12` | Verified / recorded standalone-emitter limitation; corrected scope | The `UEA_ScratchHost` wrapper has empty system scripts, so ApplyChanges validation failed on a standalone emitter despite successful module-level compilation. The original claim that the signature never updates was disproved: `AddModuleInput` with `Module.X` and `AddModuleOutput` with fully qualified `Particles.X` cleared the stale-call and output-count diagnostics. `GetStackIssues` returned no useful fix. Current handling: author inside a NiagaraSystem package and verify the target schema; standalone persistence remains a lifecycle probe, not a reason to repeatedly re-add modules. `ResolveType` uses short vocabulary (float, int/int32, bool, vec2/vector2, vec3/vector/vector3, vec4/vector4, position, color/LinearColor and the supported Array* aliases), not display names such as NiagaraFloat/Vector3f. Source history and the 2026-09-02 correction are in [Lightning LOG](../../lightning/LOG.md). |
| `LIGHTNING-K13` | Verified / UE 5.8.1 | A scratch script path is **package-private**, so `AddModule(moduleAsset=GetScratchScriptPath(...))` across packages makes the consumer unsavable. The returned refPath has the shape `/Game/A/A:UEA_ScratchHost.<Module>`; installing it in package B sets `NiagaraNodeFunctionCall.FunctionScript` to a private object of A, and every save (manual and Autosave) fails with `Illegal reference to private object: 'NiagaraSystem /Game/A/A:UEA_ScratchHost' referenced by 'NiagaraNodeFunctionCall_N' ... in its 'FunctionScript' property`, with `LogSavePackage: Warning: SavePackage Invalid Reference Diagnostics` naming the instigator. The consuming system has no scratch copy of its own — `GetScratchScriptPath(systemPath=<consumer>)` resolves through the source emitter asset and returns the same foreign path — so there is no local-path variant to substitute. Repair: `RemoveModule` that module from the consumer, independently read back `GetScriptStackTopology` (`modules: []`) and `GetCompileMessages` (zero errors), then save. The recorded getter-triggered compile/token race is addressed by the 2026-09-06 data-processing view fix. A read is never a token source; preserve exact package ownership and independently verify before saving. |
| `CORE-20260827-AUTOSAVE-SAVE-TOKEN-RACE` | Verified / recorded Satris UE 5.8.1 race | Autosave fired about 82 seconds after a create-material receipt, invalidating its five-minute token with `OUT_OF_BAND_SAVE`; the later retry reached `SAVE_TOKEN_EXPIRED`. A recorded subsequent task mutation with `allow_preexisting_dirty_save=true` obtained a valid token. Current handling: inspect the exact Autosave and dirty-package effects before continuing; save within the valid receipt lifecycle and preserve the same job identity for uncertain replay. Do not disable Autosave by default or invent a follow-up write solely to renew a token. |
| `CORE-20260902-GETCONTENT-RAW-CONVERTTOJSON-DEPTH` | Verified / Windows PowerShell 5.1 | `Get-Content -Raw` returns a PSObject carrying provider NoteProperties (`PSPath`, `PSParentPath`, `PSChildName`, `PSDrive`, `PSProvider`, `ReadCount`), not a plain String. Passing it to `ConvertTo-Json` at `-Depth` >= 4 serializes those properties recursively and grows exponentially: a 754-char HLSL body gave 4.5 KiB at depth 4, 39 KiB at 5, 475 KiB at 6, 107 MiB at 8, and never returned at 10 or 40 — two runs burned ~640 CPU-seconds each before a file-based trace located it. Controls isolate the cause: a 754-char literal, and the same body after any `-replace`, both serialize as a JSON string at every depth, because those yield a plain String; a short sentinel string is stable at depths 2-6, so this is not generic depth behavior. Fix: `[string](Get-Content -Raw ...)`, verified safe at `-Depth 40`. Prior scripts using `-Depth 40` survived only because their payloads had passed through a string operation. Recognize the symptom as: the request JSON file is never written, the process pins one core, and stdout shows nothing because the pipeline consumer is still buffering. |
| `NIAGARA-20260902-APPLYCHANGES-DIRTIES-ENGINE-PACKAGE` | Verified fix / UE 5.8.1 2026-09-06 | Scratch healing previously visited public library scripts through every stack call. GetScratchGraphFromCall now accepts only private scripts in the caller package; apply notifications also enforce ownership. Native local/shared/foreign fixtures passed. Real scratch input/HLSL/ParticleRead edits, refresh, apply and save left /Niagara/Modules/Emitter/EmitterState clean with an unchanged snapshot. Account for any new out-of-scope package before continuing; never add shared content to the save set. |
| `CORE-20260902-SAVE-TOKEN-FOLLOWS-OBSERVED-EFFECT` | Verified / current 2.0.1 | Only a verified observed change can issue a save capability; allow_preexisting_dirty_save does not manufacture a change. The 2026-09-06 engine fix stopped data-processing view initialization from compiling Niagara, so four reviewed Niagara readers now remain unchanged and are directly allow-listed. Never use a read or an unrelated grouping/property edit as a token carrier. Finish the task-required mutation lifecycle, independently verify, then use its exact token. |
| `NIAGARA-20260903-ADDEMITTER-DEEPCOPIES-SCRATCH` | Verified / UE 5.8.1 | `NiagaraToolset_System.AddEmitter(templateEmitter=<standalone NiagaraEmitter asset>)` **deep-copies** the emitter and its scratch modules into the consumer's own package, so it does not hit LIGHTNING-K13's cross-package private-reference failure: `GetScratchScriptPath` on the consumer returned `/Game/LightningToolkit/NS_LG_Bolt_V7.NS_LG_Bolt_V7:NE_Controller.NiagaraScratchPadContainer_1.LG_CtrlSpec_0`, `ListScratchModules` reported the module as owned, the graph arrived intact (6 nodes including the `Util::ExecIndex` op node), and the system compiled with zero errors. The boundary is the operation, not the asset: `AddModule` with a foreign scratch path references and breaks saving; `AddEmitter` with an emitter asset copies and is safe. Two caveats: the copied module's script object gains a numeric suffix while `ListScratchModules` reports the bare name, and the template's **user parameters are promoted to system level** — importing `NE_Controller` added 10 retired v2.16 parameters the consumer had deliberately removed. Read the consumer's user-variable set after any AddEmitter and reconcile; `NiagaraToolset_System` cannot clean the template itself because it rejects standalone emitter asset paths. |
| `NIAGARA-20260903-DANGLING-PIN-RESURRECTS-PARAMS` | Verified incident / removal smoke passed on 2026-09-06 | The original ten User parameters returned from dangling scratch MapGet pins after compile. The packaged RemoveScratchPin/RefreshModuleCallNodes route is now loaded and tested: adding then removing a disposable mapped-variable pin removed its paired default input; refresh/apply and subsequent compile/reload left the retired name absent. For an existing delivery graph, remove its actual mapped pins before deleting matching User variables and independently check the next compile. |
| `BLUEPRINT-20260904-SET-PARENT-SAVE-RESULT-UNKNOWN` | Observed on original asset / simple regression passed | The original Abyss reparent/save changed disk bytes while the package remained dirty; asynchronous compilation was not isolated as its cause. A 2026-09-06 disposable Actor-to-Pawn reparent saved successfully and preserved CDO values after restart. This does not establish a universal fix for complex Blueprint subclasses. Keep the real receipt status, independently inspect parent/dirty/persistence, and do not retry reparent or invent another mutation for a token. |
| `CORE-20260905-DAEMON-TARGET-BINDING` | Verified / offline regression; fixed in scripts | A Gateway request for mock endpoint B was forwarded to a daemon bound to A and returned A's payload. Identity probing checked only service name while forwarding dropped the resolved endpoint. Current Gateway verifies endpoint and routed session during reuse, forwards both, and the daemon checks both before dispatch. Regression verifies matching calls, rejection at both boundaries, and AutoDaemon preserving B through the one-shot path when A owns the daemon port; A receives no rejected tool calls. Existing legacy daemons must be restarted for explicit reuse. No real UE was contacted. |
| `NIAGARA-20260906-DATA-VIEW-COMPILE-SAVE-RACE` | Verified fix / UE 5.8.1 | GetStackInputData and GetEmitterTopology repeatedly replaced ScriptRuntimeCompiledDataForEditor while leaving the package clean. RefreshAll compiled even when the view disabled simulation/auto-compile. Data-processing RefreshAll now skips that compile; SetStackInputData waits for its mutation-triggered compile before returning. Repeated direct readers stayed unchanged, and an input change plus independent read and exact-token save passed. |
| `NIAGARA-20260906-SCRATCH-COMPILE-INVALIDATION` | Verified source and runtime fix / UE 5.8.1 | Generic NotifyGraphChanged does not advance Niagara compile identity. A ParticleRead binding changed in source while compilation retained the old dependency. ApplyChanges now calls the public script-source MarkNotSynchronized API for owned scratch graphs before notify/compile/wait. The following compile dropped the stale circular-dependency warning; verify compiled behavior when changing DI defaults. |
| `BLUEPRINT-20260906-INHERITED-DEFAULT-CACHE-GAP` | Verified fix / UE 5.8.1 | The cache enumerated NewVariables only, omitting an inherited InitialLifeSpan override despite successful save. The generator now emits editable inherited differences against the parent CDO under Defaults. The saved 9.25 and 11.5 values appeared in the sidecar; older sidecars without that section need live reads for those fields. |
| `CORE-20260906-EMPTY-JSON-TRANSPORT` | Verified fix / PowerShell 5.1 and live UE | Empty compile-message arrays collapsed to null and caused daemon UTF-8 serialization errors; singleton arrays also lost shape. Gateway now preserves array boundaries and explicitly serializes null, including empty receipt payloads. Direct and daemon regression cases cover empty/singleton/nested arrays, false, null and reliable receipts; real GetCompileMessages returns []. |

## Promoted rules

- Independent readback, daemon target binding, and save recovery: `references/core.md`.
- Large payload and manual visual approval: `references/core.md` and
  `references/scene-editing.md`.
- Material pin discovery: `references/materials.md`.
- Blueprint CDO scope and save limitations: `references/blueprints.md`.
- Niagara scratch capability, signature/pin cleanup, Apply diagnostics, and cross-frame validation:
  `references/niagara.md`.
