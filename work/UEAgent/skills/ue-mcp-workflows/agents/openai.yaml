interface:
  display_name: "UE MCP Workflows"
  short_description: "Gate and operate UE through MCP"
  default_prompt: "Use $ue-mcp-workflows: run the UEAgent doctor first, then choose cache, live read, or task-gated mutation from the receipt."
