{
  "action": "tool.call",
  "tool": "execute_python_code",
  "arguments": {
    "code": "import unreal\nprint([n for n in dir(unreal.NiagaraComponent) if any(k in n.lower() for k in ['activate','deactivate','active','execution_state'])])\nprint('SET_ACTIVE=' + str(unreal.NiagaraComponent.set_active.__doc__))"
  }
}
