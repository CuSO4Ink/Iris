# QQTechDigest · BACKLOG

> 只保留尚未验证的执行项；完成过程进入 `LOG.md`。

## 当前

- [ ] 观察第一条获授权群消息进入接收器并生成真实摘录。
- [ ] 收集一周误报/漏报样本后调整关键词和阈值。

## 有证据再做

- [ ] 仅在规则效果不足时接入本地或兼容 API 的 LLM 二次提炼。
- [ ] 仅在截图信息确有价值时增加图片落盘与 OCR。
- [ ] 仅在需要补历史记录时增加 `get_group_msg_history` 回溯。
