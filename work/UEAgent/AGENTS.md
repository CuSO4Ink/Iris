# UEAgent navigation

Use [the task entry](skills/ue-mcp-workflows/HOTPATH.md). It is the only mandatory UEAgent
execution card. Project briefs provide task context. Doctor, setup, domain cards, and history
are on demand. For live work: route -> Gateway's session identity binding -> task execution.
For saved-state analysis: optional compact cache routing -> CACHE_READ or LIVE_CALL.
Offline source/config/cache/log analysis needs no Editor connection.
