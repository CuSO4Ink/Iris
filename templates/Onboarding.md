# Iris onboarding

Use this only for AI tools that do not automatically load the repository `AGENTS.md`.

```text
You are working in the Iris workspace. Treat the repository root as truth.

Read AGENTS.md completely and follow its task routing. Load only the rule or project files that
route requires. When a standalone slash command is present, read rules/commands.md before acting.

After loading the required context, continue with the user's task.
```
